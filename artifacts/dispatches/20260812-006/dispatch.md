# Dispatch 20260812-006：MVP 2C Codex 主动意图循环

## Q1：目标、范围与完成标准

将 MVP 2B 的受限队列接入项目 Skill 的正常请求启动流程：健康检查通过且请求并非严格只读或仅查看工作台时，Codex 自动调用一次 `process-pending-intents --limit 10`，再继续当前任务。

非目标：后台 daemon、定时器、浏览器执行、远程访问、GitHub、审批消费、Git/发布行为。

完成标准：Skill、使用手册和合同测试明确触发条件、上限、失败语义和禁止范围；完整测试、独立验收和主线整合通过。

## Q2：风险、授权与验收

L2。Human 已要求 Codex 更自主、持续推进。自动循环只处理已由 Human 提交的受控意图，复用 MVP 2B 的每条门禁；严格只读、仅查看工作台、健康失败或队列命令非零时不进行后续写入。

## Q3：执行与审阅

Codex 负责唯一写入、测试、整合与状态维护。Claude Code 进行最终独立验收；不自动降级 Reviewer。

## Q4：隔离与上下文

- 基线：`8e38632`。
- 分支：`agent/codex/20260812-006-mvp2c-proactive-loop`。
- 允许范围：项目 Skill、使用手册、handoff、测试和本任务工件。

## Q5：状态与纠偏

队列中 `REJECTED`/`BLOCKED` 是单条业务事实，Codex 在当前请求报告它们后继续；队列命令的非零退出是控制面不健康，后续写入停止，只能提供只读说明。

## Q6：验收

仅接受 Claude V4 可解析 `PASS`。通过后写入安全 receipt、验证记录和 handoff。

## Q7：知识回流

沉淀“主动性来自受限的前台控制循环，而非隐式后台权限”的规则。MiMo 不可用时明确不执行。
